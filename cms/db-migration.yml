apiVersion: batch/v1
kind: Job
metadata:
  name: cms-prisma-migrate
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  backoffLimit: 3
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: somkumarva/cms-web:51bfd6fa0944fadf148aaee1c10823a597404bbf
          command: ["pnpm", "db:migrate"]
          envFrom:
            - secretRef:
                name: cms-env
